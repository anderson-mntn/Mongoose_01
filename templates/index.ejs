<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redirection</title>
</head>
<body>
    <h3>Salve your links! =&rpar;</h3>

    <!-- Para onde queremos enviar a req, o att name é imporante -->
    <form action="/" method="POST">
        <input class="field-input" type="text" name="title"  placeholder="title"> 
        <input class="field-input" type="text" name="description" placeholder="description"> 
        <input class="field-input" type="text" name="url"  placeholder="url"> 
        <button>Salvar</button>
    </form>

    <% if(error){ %>
       <h3><%=error.message%></h3> 
    <% } %>
</body>
</html>